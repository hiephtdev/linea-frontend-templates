import type { Address, Narrow } from 'abitype';
import type { Client } from 'viem/clients/createClient.js';
import type { Transport } from 'viem/clients/transports/createTransport.js';
import type { Chain } from 'viem/types/chain.js';
import type { ContractFunctionConfig } from 'viem/types/contract.js';
import type { MulticallContracts, MulticallResults } from 'viem/types/multicall.js';
import { type DecodeFunctionResultErrorType } from 'viem/utils/abi/decodeFunctionResult.js';
import { type EncodeFunctionDataErrorType } from 'viem/utils/abi/encodeFunctionData.js';
import { type GetChainContractAddressErrorType } from 'viem/utils/chain/getChainContractAddress.js';
import { type GetContractErrorReturnType } from 'viem/utils/errors/getContractError.js';
import type { ErrorType } from 'viem/errors/utils.js';
import type { CallParameters } from 'viem/actions/public/call.js';
import { type ReadContractErrorType } from 'viem/actions/public/readContract.js';
export type MulticallParameters<TContracts extends ContractFunctionConfig[] = ContractFunctionConfig[], TAllowFailure extends boolean = true> = Pick<CallParameters, 'blockNumber' | 'blockTag'> & {
    allowFailure?: TAllowFailure;
    /** The maximum size (in bytes) for each calldata chunk. Set to `0` to disable the size limit. @default 1_024 */
    batchSize?: number;
    contracts: Narrow<readonly [...MulticallContracts<TContracts>]>;
    multicallAddress?: Address;
};
export type MulticallReturnType<TContracts extends ContractFunctionConfig[] = ContractFunctionConfig[], TAllowFailure extends boolean = true> = MulticallResults<TContracts, TAllowFailure>;
export type MulticallErrorType = GetChainContractAddressErrorType | ReadContractErrorType | GetContractErrorReturnType<EncodeFunctionDataErrorType | DecodeFunctionResultErrorType> | ErrorType;
/**
 * Similar to [`readContract`](https://viem.sh/docs/contract/readContract.html), but batches up multiple functions on a contract in a single RPC call via the [`multicall3` contract](https://github.com/mds1/multicall).
 *
 * - Docs: https://viem.sh/docs/contract/multicall.html
 *
 * @param client - Client to use
 * @param parameters - {@link MulticallParameters}
 * @returns An array of results with accompanying status. {@link MulticallReturnType}
 *
 * @example
 * import { createPublicClient, http, parseAbi } from 'viem'
 * import { mainnet } from 'viem/chains'
 * import { multicall } from 'viem/contract'
 *
 * const client = createPublicClient({
 *   chain: mainnet,
 *   transport: http(),
 * })
 * const abi = parseAbi([
 *   'function balanceOf(address) view returns (uint256)',
 *   'function totalSupply() view returns (uint256)',
 * ])
 * const results = await multicall(client, {
 *   contracts: [
 *     {
 *       address: '0xFBA3912Ca04dd458c843e2EE08967fC04f3579c2',
 *       abi,
 *       functionName: 'balanceOf',
 *       args: ['0xA0Cf798816D4b9b9866b5330EEa46a18382f251e'],
 *     },
 *     {
 *       address: '0xFBA3912Ca04dd458c843e2EE08967fC04f3579c2',
 *       abi,
 *       functionName: 'totalSupply',
 *     },
 *   ],
 * })
 * // [{ result: 424122n, status: 'success' }, { result: 1000000n, status: 'success' }]
 */
export declare function multicall<TContracts extends ContractFunctionConfig[], TChain extends Chain | undefined, TAllowFailure extends boolean = true>(client: Client<Transport, TChain>, args: MulticallParameters<TContracts, TAllowFailure>): Promise<MulticallReturnType<TContracts, TAllowFailure>>;
//# sourceMappingURL=multicall.d.ts.map